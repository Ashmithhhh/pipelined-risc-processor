# Export these variables in your shell before running `npm run serve`.
# This file is documentation only; the server does not automatically load it.
GEMINI_API_KEY=replace_with_your_google_ai_studio_key
GEMINI_MODEL=gemini-2.5-flash
PORT=8000
HOST=127.0.0.1
